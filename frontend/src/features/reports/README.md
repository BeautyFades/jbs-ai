# Reports (planned)

Custom, Qlik-like reports over governed dbt models: chart builder, filter panel,
saved views. Will expose `reports.*` AI tools (open report, apply filter, explain
chart) through `@/ai/tool-registry` so the assistant can drive it.
