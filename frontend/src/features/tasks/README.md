# Tasks (planned)

Kanban board for operational follow-ups. Will expose `tasks.*` AI tools
(create task, move card, summarize board) through `@/ai/tool-registry` so the
assistant can create and manage tasks from any conversation.
