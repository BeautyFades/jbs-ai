"""Feature modules. Each is a self-contained router + service + repository
with strict boundaries, so any module can later be extracted to its own
service without touching the others. See docs/ARCHITECTURE.md."""
